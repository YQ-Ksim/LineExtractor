{
  "scripts": {
    "build:ts": "tsc -p tsconfig.app.json && tsc -p tsconfig.worker.json",
    "sync:js": "node tools/sync-build.cjs",
    "build": "npm run build:ts && npm run sync:js",
    "typecheck": "npm run build:ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
